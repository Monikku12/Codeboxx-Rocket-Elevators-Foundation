<%= form_with(model: dim_customer, local: true) do |form| %>
  <% if dim_customer.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(dim_customer.errors.count, "error") %> prohibited this dim_customer from being saved:</h2>

      <ul>
      <% dim_customer.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :creation_date %>
    <%= form.date_select :creation_date %>
  </div>

  <div class="field">
    <%= form.label :company_name %>
    <%= form.text_area :company_name %>
  </div>

  <div class="field">
    <%= form.label :full_name %>
    <%= form.text_area :full_name %>
  </div>

  <div class="field">
    <%= form.label :company_contact_email %>
    <%= form.text_area :company_contact_email %>
  </div>

  <div class="field">
    <%= form.label :Nb_Elevator %>
    <%= form.number_field :Nb_Elevator %>
  </div>

  <div class="field">
    <%= form.label :customer_city %>
    <%= form.text_area :customer_city %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
